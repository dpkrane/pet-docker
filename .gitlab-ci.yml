include:
  - project: dpkrane-includes/gitlab-ci
    file: build-docker.yml
    ref: v.0.02
stages:
  - build

build_nginx:
  extends: .build-image

build_backend:
  extends: .build-image

build_frontend:
  extends: .build-image
